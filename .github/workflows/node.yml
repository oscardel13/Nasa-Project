name: NASA Project CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    runs:
        using: 'node16'
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build --prefix client